{
  "name": "3d-job-composer",
  "version": "0.3.0",
  "private": true,
  "description": "Local Windows tool for composing standardized 3D job packages",
  "type": "module",
  "scripts": {
    "start": "node src/server/server.js",
    "dev": "node --watch src/server/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

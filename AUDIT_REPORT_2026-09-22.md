# 3DJobComposer 설계단 보고용 현황 감사

- 기준일: 2026-09-22
- 대상: `C:\_InternalProjects\3DJobComposer`
- 기준 문서: `DESIGN_HANDOFF.md`, `README.md`
- 버전: Composer `0.2.1`, manifest schema `1.1`
- 환경: Windows / 시스템 Node.js `v24.21.0`
- 방식: 소스·문서 검토, 기존 테스트 실행, 격리된 합성 Job의 서비스·HTTP 재현
- 변경 범위: 본 보고서와 `.tmp/` 감사 증거만 추가. 기존 소스·문서·테스트·템플릿 14개 파일의 SHA-256 변경 0개. 실제 사용자 Job에는 접근하거나 SAVE하지 않음.

## 1. 종합 판정

**설계 인계 및 개발 재개는 가능하다. 기본 작성·생성·로드·저장 기능은 동작하지만, 기존 Job 편집의 데이터 보존과 저장 실패 복구까지 완료된 안정화 버전으로 판정할 수는 없다.**

제품의 역할은 설계와 일치한다. 자연어와 사용자 Reference를 패키징하며, 모델링·조사·ImageGen·DCC 실행을 담당하지 않는다. 기본 경로의 자동 테스트는 모두 통과한다. 다만 정상 경로 테스트 밖에서 반복 저장, 일부 저장 실패, Load→Save 변환, 미래 schema 저장의 문제가 확인되었다.

본 보고서의 P1/P2는 감사자가 제안하는 대응 우선순위이며 정식 출시 기준이 아니다. P1은 데이터 일관성 또는 로컬 쓰기 경계의 우선 검토, P2는 사용 흐름·보존 정책·호환 계약 보완을 뜻한다. 모든 항목은 미수정 상태다.

## 2. 검증 결과와 증거 수준

| 검증 | 결과 | 범위 및 한계 |
|---|---|---|
| 기존 `node --test` | 9 PASS / 0 FAIL | 서비스 계층 중심의 정상 경로 회귀 테스트 |
| 전체 `src/**/*.js` 문법 검사 | PASS | 실제 브라우저 실행과 별개 |
| `/api/config` | HTTP 200 | 0.2.1 / 1.1 / 시스템 Node 일치 |
| HTTP 생성→로드→저장 | 201→200→200 | 합성 Job |
| 같은 이름 신규 생성 | HTTP 409 | 기존 Job 덮어쓰기 거부 |
| 무효 편집 토큰 저장 | HTTP 403 | 서버 경계의 토큰 검사 확인 |
| 누락 Reference 로드→저장 | 경고 및 manifest 항목 보존 | 합성 파일을 별도 이름으로 옮겨 재현 |
| `../outside.png` Reference 저장 | 거부 | 일반 상대경로 이탈만 검사; junction/symlink 미검증 |
| 반복 저장 및 장애 상황 | 아래 7개 항목 확인 | 정상 회귀 테스트 PASS와 별개 |
| 실제 브라우저 UI/console | 미검증 | 클릭, preview, 레이아웃, UI end-to-end PASS 아님 |
| 배치파일 더블클릭/Explorer | 미검증 | 런처 코드의 시스템 Node 선택만 검토 |
| 대용량·동시 저장 부하·강제 종료 | 미검증 | 동시 편집은 두 개의 오래된 스냅샷을 순차 저장해 검사 |

추가 재현 스크립트는 13개의 결과 레코드를 출력한다. 이는 13개의 정상 테스트 통과를 뜻하지 않는다. 결함 재현과 정상 동작 관찰이 함께 포함되어 있다.

## 3. 주요 발견 사항

### F02 · P1 · 저장 실패 후 manifest와 TASK의 내용 불일치

**증거: 런타임 장애 주입으로 재현.**

- 위치: `src/server/job-service.js:251-262`
- 현재 SAVE는 임시 파일 작성 후 두 최종 파일에 `copyFile`을 병렬 수행한다. 전체 저장을 원상 복구하는 절차는 없다.
- 합성 Job에서 manifest 복사는 완료시키고 TASK 복사에 `EACCES`를 주입했다. 저장은 실패했지만 manifest 설명은 `NEW DESCRIPTION`, TASK는 이전 설명으로 남았다.
- 결과적으로 “저장 실패”가 “아무것도 바뀌지 않음”을 의미하지 않는다. 신규 Reference도 이보다 먼저 기록되므로 실패 시 남을 수 있다.
- 신규 CREATE의 staging/rename 설계와 기존 SAVE의 보장 수준을 구분해야 한다. 설계 문서는 CREATE 원자성을 명시하지만 SAVE 복구 기준은 정하지 않았다.
- 권고: manifest를 기준으로 한 복구 전략, 이전 버전 보관, 저장 완료 표식 등 실패 후 일관성을 회복할 계약을 먼저 정한다. 파일별 rename만으로 두 파일 전체의 원자성이 확보되는 것은 아니다.
- 수용 기준: 각 저장 단계의 실패 후 파일 상태가 정의된 이전 상태 또는 복구 가능한 새 상태로 수렴하고, 사용자에게 실제 상태를 알릴 것.

### F06 · P1 검토 · 외부 Origin의 쓰기 요청을 서버가 수용

**증거: 합성 HTTP 요청 수용 재현. 실제 브라우저 공격 재현은 하지 않음.**

- 위치: `src/server/server.js:32-45`, `67-89`
- `Origin: https://audit.invalid`, `Content-Type: text/plain`인 JSON 본문을 생성 API에 보냈고 HTTP 201 및 합성 Job 생성이 확인되었다.
- 기본 loopback 바인딩은 외부 네트워크 직접 접속을 제한하지만 요청을 보낸 웹 페이지의 출처 검사를 대신하지 않는다. 신규 생성 API에는 별도 쓰기 토큰도 없다.
- edit token은 LOAD 이후 저장 대상 폴더를 고정하는 장치이지 전체 API의 사용자 인증 장치가 아니다.
- 브라우저의 로컬 네트워크 접근 정책 및 실제 공격 가능성은 이번 감사 범위에서 검증하지 않았다. 외부 사이트가 응답을 읽을 수 있다는 뜻도 아니다.
- 권고: 허용 Origin/Host 및 요청 Content-Type, 쓰기 요청 토큰의 정책을 정의한다. 로컬 개인 도구에 맞는 최소 보호로 제한하고 외부 계정 기능과 결합할 필요는 없다.
- 수용 기준: 정상 앱 출처의 요청은 성공하고 허용되지 않은 출처의 변경 요청은 파일을 생성하기 전에 거부될 것.

### F01 · P2 · 새 Reference 추가 후 반복 SAVE하면 파일이 계속 복사됨

**증거: 서비스 재현 + UI 코드 경로 확인. 브라우저 클릭 재현은 미실시.**

- 위치: `src/web/app.js:126-137`, `349-370`; `src/server/server.js:113-116`; `src/server/job-service.js:238-247`
- 기존 Job에 신규 Reference를 추가한 동일 payload를 두 번 저장하면 manifest의 파일 경로가 `references/ref_001.png`에서 `references/ref_002.png`로 바뀌고 두 파일이 모두 남는다.
- UI는 저장 후 신규 Reference의 `file`을 해제하거나 서버의 `existingFile` 경로로 전환하지 않는다. 서버 응답에도 해당 매핑이 없다.
- 원본 파괴는 없지만 반복 저장할 때 불필요한 파일과 경로 변경이 발생한다. 고아 파일 보존 정책 때문에 자동 회수도 되지 않는다.
- 권고: 저장 성공 후 서버가 확정한 Reference 매핑으로 편집 상태를 갱신한다. 오래된 파일 자동 삭제는 기존 설계 원칙에 따라 추가하지 않는다.
- 수용 기준: 첫 저장 후 변경 없는 재저장에서 Reference 파일 수와 manifest 경로가 변하지 않을 것.

### F03 · P2 · 수정 없는 Load→Save에서도 데이터가 변경됨

**증거: 합성 Job 왕복 저장으로 재현.**

- 위치: `src/server/job-service.js:134-138`, `207`, `221`; `src/server/job-schema.js:139-144`
- `work_scope: []`로 생성한 Job을 Load→Save하면 `["Unspecified"]`로 바뀐다. 빈 배열도 fallback을 적용하기 때문이다.
- 이름 `Original : Name`으로 생성한 manifest의 `job.original_name`이 Load→Save 이후 사라진다. 로드에는 정제된 `job.name`만 전달된다.
- 두 변화 모두 이 재현에서 경고가 없었다. 설명 원문이 사라진 것은 아니며 Job Name 메타데이터의 손실이다.
- 권고: “값이 없음”과 “명시적 빈 배열”의 의미, `original_name`의 보존 의무를 정한다. 현재 문서가 빈 목록을 허용하므로 특히 왕복 저장 의미를 명확히 해야 한다.
- 수용 기준: 수정 없는 저장의 허용 변환을 명세하고, 나머지 지원 필드와 원본 이름은 보존할 것.

### F04 · P1 검토 · 미래 schema를 저장하면 현재 schema로 축소됨

**증거: 합성 schema 2.0 manifest에서 재현. 미래 실데이터가 유실됐다는 뜻은 아님.**

- 위치: `src/server/job-service.js:152`, `202-225`, `251`; `src/server/job-schema.js:146-177`
- schema를 `2.0`으로 지정하고 미지원 Quality 및 `custom_extension`을 넣은 합성 manifest를 로드했다.
- schema/Quality 경고는 나왔지만 SAVE는 허용됐다. 저장 후 schema는 `1.1`, Quality는 `Unspecified`, 사용자 확장 필드는 제거됐다.
- “가능한 필드를 복원”하는 문서 정책은 구현되어 있다. 그러나 호환 로드가 안전한 편집·저장을 보장하지 않으며, 경고 문구만으로 전체 손실 범위가 설명되지 않는다.
- 권고: 미래 schema는 읽기 전용으로 열거나, 변환 전 원본을 보존하는 별도 명시적 변환으로 처리한다. schema 1.0의 지원 migration과 구분한다.
- 수용 기준: 미지원 schema를 일반 SAVE로 조용히 축소하지 않을 것.

### F05 · P2 · 비활성 AI 항목의 미완성 값이 Job 저장을 막음

**증거: HTTP 400 재현 + UI 사전 검사 확인.**

- 위치: `src/server/job-schema.js:73-103`; `src/web/app.js:353-354`
- Package `enabled=false`, Item `enabled=false`라도 `Specific Part`의 Target Part가 비어 있으면 전체 Job 생성이 거부된다.
- UI에서도 package/item 활성 여부와 무관하게 검사하므로 Package를 꺼도 미완성 항목이 남으면 저장을 진행할 수 없다.
- 이는 현재 구현 동작이며, 문서는 비활성 초안의 검증 정책을 명시하지 않는다.
- 권고: 비활성 항목을 초안으로 보존할지, 항상 완성된 데이터만 허용할지 결정한다. 후자를 택한다면 숨겨진 항목이 저장을 막는 이유를 UI에서 드러내야 한다.
- 수용 기준: 비활성 상태의 의미 및 저장 허용 조건이 문서·UI·API에서 일치할 것.

### F07 · P2 · 오래된 편집본 저장이 더 최근 수정을 덮어씀

**증거: 두 개의 로드 스냅샷을 순차 저장하여 재현. 실제 병렬 I/O 부하 테스트와 다름.**

- 위치: `src/server/server.js:98`, `110-113`; `src/server/job-service.js:229-263`
- A/B가 같은 Job을 로드한 뒤 A가 설명을 수정해 저장하고 B가 예전 데이터를 저장하면 A의 수정이 사라진다.
- edit token에는 폴더 경로만 연결되어 있고 revision/hash 검사는 없다. 외부 편집기의 변경에도 같은 정책 문제가 있다.
- 단일 작성자 전용인지, 여러 탭 또는 외부 작업자와 공존해야 하는지 설계에 명시되지 않았다.
- 권고: 단일 편집 제한을 정하거나 로드 시점 revision과 저장 직전 revision을 비교한다.
- 수용 기준: 더 최근 데이터가 존재하면 충돌을 알려주거나 명시된 편집 정책으로 보호할 것.

## 4. 설계 계약과 구현의 일치 범위

| 설계 계약 | 판단 |
|---|---|
| 자연어·Reference 패키징에 책임 한정 | 코드와 일치. 실제 AI/DCC 실행 없음 |
| 설명/Reference Note 원문 보존 | 검토한 정규화·렌더링 경로와 기존 테스트에서 확인 |
| 사용자에게 구조화 판단 강제하지 않음 | UI는 Unspecified/빈 배열을 자동 제공하므로 기본 사용 흐름 충족 |
| 최소 3필드만 있는 API 요청 | 미지원. 이름·설명·root만 전송하면 `asset.category` 오류로 HTTP 400. UI의 선택 입력과 API 필드 생략을 구분해 문서화 필요 |
| 신규 Job 덮어쓰기 금지 | 일반 중복 생성 거부 확인. 동시 CREATE 경쟁 상황 미검증 |
| 명시적 Load 후 SAVE | HTTP 토큰 경계 확인. 서비스 함수 직접 호출에는 토큰 검사 없음; 계층 분리 자체는 문제 아님 |
| manifest가 canonical | 로드는 manifest만 사용하고 TASK를 파싱하지 않음 |
| 기존 RUN_LOG 및 제거된 Reference 보존 | 기존 회귀 테스트에서 확인 |
| 누락 Reference 경고/보존 | 추가 재현으로 확인 |
| schema 1.0 호환 | 기존 회귀 테스트에서 확인 |
| 미래 schema 호환 | 읽기 복원과 안전한 저장은 구분 필요, F04 |
| AI Reference Package는 계획 데이터 | 일치. 비활성 초안 검증 정책은 F05 |

## 5. 테스트 공백

기존 테스트 파일은 9개의 서비스 테스트로 구성되어 있다. 특히 “saves edits only after load”라는 테스트명은 서비스 함수를 직접 호출하며, HTTP 토큰 거부를 검증하지 않는다. 이번 감사에서 HTTP 무효 토큰 거부를 별도로 확인했다.

지속적인 회귀 방지를 위해서는 F01 반복 저장, F02 장애 복구, F03 무수정 왕복, F04 미지원 schema 저장, F05 비활성 항목, F06 출처 검사, F07 편집 충돌을 정책 확정 후 테스트에 포함해야 한다. 이번 감사용 스크립트는 결함의 현재 동작을 기록하는 도구이며 정식 회귀 테스트를 대체하지 않는다.

브라우저 E2E, 이미지 실제 표시, Explorer 실행, 150 MB 경계/대용량 메모리, 시스템 종료 중 복구, junction/symlink 경계는 이번에 PASS로 판정하지 않았다.

## 6. 인계 상태 및 권장 순서

현재 프로젝트는 Git 저장소가 아니므로 이전 계정의 commit/branch 기준을 확인할 수 없다. 이 감사는 전달된 파일 스냅샷을 기준으로 한다. 앱 코드에는 계정 로그인이나 cloud 실행 의존성이 없으며, 설계단의 과거 대화 전체가 파일로 이전되었다는 보장은 별개다.

권장 순서:

1. 기존 Job SAVE의 실패 복구와 미지원 schema 저장 정책을 결정한다(F02/F04).
2. 로컬 변경 API의 출처 보호를 보완한다(F06).
3. 반복 저장 Reference 매핑과 무수정 왕복 보존을 수정한다(F01/F03).
4. 비활성 초안과 편집 충돌 정책을 확정한다(F05/F07).
5. 해당 회귀 테스트 및 실제 브라우저 검증을 수행한다.
6. 이후 Folder Picker, Reference 정렬, AI 실행 계층 등의 기능 확장을 검토한다.

**설계단 전달 요약:** 핵심 authoring/packaging 경계와 정상 기능은 구현되었고 9개 기존 테스트는 통과했다. 안정화의 주요 잔여 과제는 기존 Job 저장의 일관성·왕복 보존·호환 저장·로컬 쓰기 경계다. 이번 감사에서는 구현 수정 및 실제 사용자 Job 변경을 하지 않았다.

## 7. 재현 및 증거 파일

- 감사 재현 스크립트: `.tmp/audit-20260922.mjs`
- 결과 JSON: `.tmp/audit-results-20260922.json`
- 원본 파일 SHA-256: `.tmp/audit-baseline-20260922.json`
- 합성 Job 위치: 결과 JSON의 `fixtureRoot` 참조
- 재실행: `& 'C:\Program Files\nodejs\node.exe' .tmp/audit-20260922.mjs`

재실행은 `.tmp/`에 새로운 합성 Job 디렉터리를 만들며 결과 JSON을 갱신한다. 장애 주입은 감사 Node 프로세스 내부에서만 적용되고 복원된다. 임시 HTTP 서버는 검사 후 닫힌다. 외부 Origin 검사는 헤더만 사용하며 해당 외부 도메인에 접속하지 않는다. `.tmp/`는 인계 시 제외 가능한 폴더이므로 재현 자료까지 전달하려면 이 파일들을 함께 보관해야 한다.
